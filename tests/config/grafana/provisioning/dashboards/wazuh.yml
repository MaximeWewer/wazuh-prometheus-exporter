apiVersion: 1

providers:
  - name: wazuh
    type: file
    disableDeletion: false
    allowUiUpdates: true
    options:
      # The project dashboards (grafana/dashboards) are mounted here by compose.
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
